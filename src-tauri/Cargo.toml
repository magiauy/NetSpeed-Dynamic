[package]
name = "netspeed-dynamic"
version = "1.0.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

[lib]
name = "netspeed_dynamic_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["macos-private-api", "tray-icon"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
# 添加 sysinfo 库来监控网络
sysinfo = "0.30"
windows-sys = { version = "0.59", features = [
    "Win32_Graphics_Dwm",
    "Win32_Graphics_Gdi",
    "Win32_Foundation",
    "Win32_UI_WindowsAndMessaging",
    "Win32_UI_Controls",
] }
tauri-plugin-single-instance = "2.0.0" # 根据你的Tauri2版本调整具体小版本
tokio = { version = "1", features = ["full", "rt-multi-thread"] }
urlencoding = "2.1.3"
lazy_static = "1.5.0"
chrono = { version = "0.4", default-features = false, features = ["std", "clock"] }
[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-autostart = "2"
winapi = { version = "0.3", features = ["winuser", "windef", "shellapi", "winbase", "handleapi", "libloaderapi"] }
reqwest = { version = "0.12", features = ["json"] }
windows = { version = "0.58.0", features = [
    "UI_Notifications_Management", 
    "UI_Notifications", 
    "Foundation", 
    "Foundation_Collections",
    "ApplicationModel",
    "implement",
    "Win32_Media_Audio",
    "Win32_Media_Audio_Endpoints",
    "Win32_System_Com",
    "Win32_Foundation",
    "Win32_UI_WindowsAndMessaging",
    "Media_Control",
    "Storage_Streams",
    "Win32_System_Power",
] }
cpal = "0.15"
realfft = "3"
rand = "0.8"
tokio-tungstenite = "0.21"
futures-util = "0.3"
surge-ping = "0.8"
# 活动池 HTTP 入站服务（127.0.0.1:47300）
axum = { version = "0.8", features = ["json", "tokio"] }
