/**
 * Global lyric configuration and feature flags.
 *
 * - ENABLE_WORD_BY_WORD_LYRICS:
 *   Set to `true` to enable Word-by-Word karaoke sync mode (word highlighting and progressive word scrolling).
 *   Set to `false` to display in Line-by-Line sync mode (smooth full-line display with 2s hold & 300ms lead-in).
 */
export const ENABLE_WORD_BY_WORD_LYRICS = false;
